specVersion: "v1beta"
serviceId: "my-diet-plan-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "my-diet-plan-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
